package com.athena.mis.application.model

class ListItemCategoryNonInvActionServiceModel {
    public static final String SQL_LIST_ITEM_CATEGORY_NON_INVENTORY_MODEL = """
    DROP TABLE IF EXISTS list_item_category_non_inv_action_service_model;
    DROP VIEW IF EXISTS list_item_category_non_inv_action_service_model;
    CREATE OR REPLACE VIEW list_item_category_non_inv_action_service_model AS
        SELECT
            item.id,
            item.version,
            item.name,
            item.code,
            item.unit,
            item.item_type_id,
            item.category_id,
            item.company_id,
            it.name AS item_type_name
        FROM Item
        LEFT JOIN item_type it ON it.id = item.item_type_id
    """
    long id                             // primary key (Auto generated by its own sequence)
    long version                        // entity version in the persistence layer
    String name                         // Unique item name by category
    String code                         // Item Code
    String unit                         // Item Unit
    long itemTypeId                     // ItemType.id
    long categoryId                     // SystemEntity.id (Inventory, NonInventory, FixedAsset)
    long companyId                      // Company.id
    String itemTypeName

    static mapping = {
        cache usage: "read-only"
    }
}
